PORT=3334
CORS_ORIGIN=http://localhost:5173
# Ghostfolio API base URL (no trailing slash). Use production URL when Ghostfolio is deployed.
GHOSTFOLIO_API_URL=https://agentforge-production-e263.up.railway.app
# Set one of these so the UI works without any manual auth step:
# Option A (recommended): your Ghostfolio access token. The agent auto-exchanges it for a JWT.
GHOSTFOLIO_ACCESS_TOKEN=
# Option B: paste a JWT directly (skips the exchange step).
GHOSTFOLIO_JWT=

ANTHROPIC_API_KEY=
AGENT_MODEL=claude-sonnet-4-20250514
AGENT_MAX_TOKENS=4096
AGENT_TEMPERATURE=0.2
AGENT_DEFAULT_LOOKBACK_DAYS=30
AGENT_ALLOW_EDU=true
AGENT_DEFAULT_ACCOUNT_ID=
AGENT_ENABLE_MARKET=false
AGENT_VALUATION_FALLBACK=cost_basis

LANGSMITH_TRACING=false
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=ghostfolio-agent

LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_BASE_URL=https://cloud.langfuse.com
